[project]
name = "generativerecs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.8.0",
    "vector-quantize-pytorch>=1.23.2",
]

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
]
